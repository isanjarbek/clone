import { makeStyles } from '@material-ui/core';

export const MenuNavStyle = makeStyles((theme) => ({
    root: {
        display: 'flex',
    },
    drawerPaper: {
        width: "100%",
        backgroundColor: 'pink'
    },

}));